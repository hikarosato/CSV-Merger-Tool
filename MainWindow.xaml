<Window x:Class="CSV_Merger_Tool.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="CSV Merger Tool 1.3 © Галицький Розбишака" Height="290" Width="600"
        ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border x:Name="borderNew" Grid.Column="0" Grid.Row="0" Height="100" BorderThickness="1" BorderBrush="Gray" Background="#FAFAFA"
                AllowDrop="True" Drop="NewCsv_Drop" CornerRadius="10" DragEnter="Border_DragEnter" DragLeave="Border_DragLeave" DragOver="Border_DragOver"
                HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="10,0,10,5">
            <Grid>
                <TextBlock x:Name="TxtNewCsv" Text="Перетягніть новий CSV" TextAlignment="Center" VerticalAlignment="Center"
                           HorizontalAlignment="Center" TextWrapping="Wrap"/>
            </Grid>
        </Border>
        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Огляд" Width="75" Click="BtnNew_Click" Margin="0,0,15,0"/>
            <Button x:Name="BtnClearNew" Content="Очистити" Width="75" Click="BtnClearNew_Click" IsEnabled="False"/>
        </StackPanel>
        <Border x:Name="borderOld" Grid.Column="1" Grid.Row="0" Height="100" BorderThickness="1" BorderBrush="Gray" Background="#FAFAFA"
                AllowDrop="True" Drop="OldCsv_Drop" CornerRadius="10" DragEnter="Border_DragEnter" DragLeave="Border_DragLeave" DragOver="Border_DragOver"
                HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="10,0,10,5">
            <Grid>
                <TextBlock x:Name="TxtOldCsv" Text="Перетягніть старий CSV із перекладом" TextAlignment="Center" VerticalAlignment="Center"
                           HorizontalAlignment="Center" TextWrapping="Wrap"/>
            </Grid>
        </Border>
        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Огляд" Width="75" Click="BtnOld_Click" Margin="0,0,15,0"/>
            <Button x:Name="BtnClearOld" Content="Очистити" Width="75" Click="BtnClearOld_Click" IsEnabled="False"/>
        </StackPanel>
        <GroupBox Header="Режим об’єднання" HorizontalAlignment="Center" Grid.ColumnSpan="2" Grid.Row="2" Margin="0,5,0,0">
            <StackPanel HorizontalAlignment="Center" Margin="5">
                <RadioButton x:Name="RbModeID" Content="за ID" IsChecked="True" VerticalContentAlignment="Bottom"/>
                <RadioButton x:Name="RbModeText" Content="за оригінальним текстом" Margin="0,5,0,0" VerticalContentAlignment="Bottom"/>
            </StackPanel>
        </GroupBox>
        <Button x:Name="BtnMerge" Content="Об’єднати" Width="85" Grid.ColumnSpan="2" Grid.Row="3" HorizontalAlignment="Center" Margin="0,5,0,0"
                Click="BtnMerge_Click" IsEnabled="False"/>
    </Grid>
</Window>